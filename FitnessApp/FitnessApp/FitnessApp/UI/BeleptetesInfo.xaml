<UserControl x:Class="FitnessApp.UI.BeleptetesInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessApp.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel>
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0">Bérlet:</Label>
            <Label Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" />

            <Label Grid.Row="1">Érvényesség:</Label>
            <Label Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" />

            <Label Grid.Row="2">Hányadik használat:</Label>
            <Label Grid.Row="2" Grid.Column="1" Margin="0,0,0,10" />

            <Label Grid.Row="3">Név:</Label>
            <Label Grid.Row="3" Grid.Column="1" Margin="0,0,0,10" />

        </Grid>

    </StackPanel>

</UserControl>
